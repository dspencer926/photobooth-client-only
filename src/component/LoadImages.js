import resizeImage from '../arrows.png';
import rotateImage from '../loop.png';

const resize = new Image();
resize.src = resizeImage;

const rotate = new Image();
rotate.src = rotateImage;

export default {resize, rotate};